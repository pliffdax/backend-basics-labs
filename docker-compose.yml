services:
  mysql:
    image: mysql:8.4
    container_name: backend-labs
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: lab1
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
